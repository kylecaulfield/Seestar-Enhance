# `app/data/` — bundled data files

## `gaia_bright.parquet`

A bright-star subset of Gaia DR3, used by the SPCC stage to calibrate
colour against real star measurements. Generated by
`backend/scripts/fetch_gaia.py` — see its docstring for details.

> **Note on the bundled file.** The Parquet currently checked into
> the repository is a **regional subset**, generated via
> cone-search (radius 1.5°, G < 14) around each of the six sample
> FITS pointings — NGC 6888, NGC 2244, NGC 6960, NGC 6992, M81,
> M92. ~32,700 rows, ~1 MB. This is enough to test SPCC end-to-end
> on the committed samples but will not calibrate arbitrary targets.
>
> For a **full-sky** bundle, re-run
> `python backend/scripts/fetch_gaia.py` (default `G < 12`, ~5 M rows,
> ~30 MB) and commit the replacement. The bundled regional subset
> was created when the ESA Gaia archive was experiencing multi-hour
> outages and only small cone-searches via the Vizier mirror were
> landing reliably.

Schema (Parquet, snappy compression):

| Column              | Type    | Units         | Notes                                 |
|---------------------|---------|---------------|---------------------------------------|
| `ra`                | float64 | degrees ICRS  |                                       |
| `dec`               | float64 | degrees ICRS  |                                       |
| `phot_g_mean_mag`   | float32 | Gaia G mag    | Main broadband                        |
| `phot_bp_mean_mag`  | float32 | Gaia BP mag   | Blue channel (330-680 nm)             |
| `phot_rp_mean_mag`  | float32 | Gaia RP mag   | Red channel (630-1050 nm)             |

License: Gaia DR3 is released under [CC0 1.0](
https://creativecommons.org/publicdomain/zero/1.0/). Safe to bundle.

### Regenerating

```sh
cd backend
python scripts/fetch_gaia.py          # default: G < 12, auto mirror
GAIA_G_LIMIT=13 python scripts/fetch_gaia.py
GAIA_SOURCE=vizier python scripts/fetch_gaia.py
```

If the ESA Gaia TAP service is down, the script automatically falls
back to the CDS Vizier mirror of the same table.
