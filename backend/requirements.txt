fastapi==0.115.0
# httpx is the transport `fastapi.testclient.TestClient` runs on top of.
# FastAPI lists it as an *optional* dep (in its `all` extra, not the
# base install), so a fresh pip install with the line above alone
# leaves test_api.py uncollectable: `from fastapi.testclient import
# TestClient` raises ImportError, pytest exits 2, and CI fails.
httpx==0.28.1
python-multipart==0.0.12
uvicorn[standard]==0.30.6
astropy==6.1.4
numpy==1.26.4
scipy==1.13.1
scikit-image==0.24.0
# tifffile is a transitive dep of scikit-image but the export stage
# imports it directly for 16-bit RGB TIFF output, so pin it explicitly
# rather than leaning on the transitive resolution.
tifffile==2024.8.30
Pillow==10.4.0
pypng==0.20220715.0
colour-demosaicing==0.2.5
bm3d==4.0.3
pytest==8.3.3
hypothesis==6.115.5
# v2 — SPCC (Phase 1). astroquery fetches the Gaia DR3 subset via
# the ESA TAP service; pyarrow reads/writes the Parquet bundle that
# ships in backend/app/data/.
astroquery==0.4.7
pyarrow==17.0.0
